#include "main.h"
/**
 * mul - multiplies two integers
 * @a: integer to multiply with b
 * @b: integer to multiply with a
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
